import React from "react"
import {NavLink} from "react-router-dom"

function Header () {
    return(
        <div>
            <NavLink to ="/">Insert Image here instead on CSS</NavLink>
        </div>
    )
}

export default Header